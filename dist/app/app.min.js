/* global angular */
angular.module('app', []).factory('test', [
  '$http',
  function ($http) {
    return 5;
  }
]).factory('f223344', [
  '$http',
  '$inject',
  function ($http, $inject) {
    return 5;
  }
]);